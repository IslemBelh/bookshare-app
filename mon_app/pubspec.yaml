name: mon_app
description: "Application BookShare - Gestion de bibliothèque"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^2.24.0
  firebase_auth: ^4.17.3
  cloud_firestore: ^4.9.3
  firebase_storage: ^11.5.3
  flutter_riverpod: ^2.4.9
  firebase_messaging: ^14.7.1
  google_fonts: ^6.1.0
  # flutter_local_notifications: ^17.0.0  # ⭐ COMMENTÉ POUR L'INSTANT

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true